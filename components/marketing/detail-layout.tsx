import Link from "next/link";
import { SiteHeader, SiteFooter, FinalCTA } from "./site-header";
export function DetailLayout({label,title,description,children}:{label:string;title:string;description:string;children:React.ReactNode}){return <div className="cm-site"><a className="cm-skip" href="#main-content">Skip to content</a><SiteHeader/><main id="main-content"><section className="cm-feature-hero"><div className="cm-container"><nav className="cm-breadcrumb" aria-label="Breadcrumb"><Link href="/">Home</Link><span>/</span><span>{label}</span></nav><div style={{maxWidth:850}}><p className="cm-eyebrow">{label}</p><h1>{title}</h1><p className="cm-lead">{description}</p><Link href="/signup" className="cm-button">Start your 14-day free trial →</Link></div></div></section>{children}<FinalCTA/></main><SiteFooter/></div>}
export function Questions({items}:{items:readonly (readonly [string,string])[]}){return <section className="cm-section"><div className="cm-container cm-faq-layout"><div><p className="cm-eyebrow">THE DETAILS THAT MATTER</p><h2>Your questions,<br/>answered.</h2></div><div className="cm-faq-list">{items.map(([q,a])=><details key={q}><summary>{q}<span aria-hidden="true">+</span></summary><p>{a}</p></details>)}</div></div></section>}
