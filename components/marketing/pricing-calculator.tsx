"use client";
import { useState } from "react";
export function PricingCalculator(){const [employees,setEmployees]=useState(10);return <div className="cm-calculator"><label htmlFor="employee-count">Your active employees <strong>{employees}</strong></label><div className="cm-count-control"><button aria-label="Remove one active employee" disabled={employees<=1} onClick={()=>setEmployees(n=>n-1)}>−</button><input id="employee-count" type="range" min="1" max="100" value={employees} onChange={e=>setEmployees(Number(e.target.value))}/><button aria-label="Add one active employee" disabled={employees>=100} onClick={()=>setEmployees(n=>n+1)}>+</button></div><div><span>Estimated monthly total</span><output htmlFor="employee-count" aria-live="polite">A${employees*10}<small> / month</small></output></div><p>{employees} active {employees===1?"employee":"employees"} × A$10 per month</p></div>}
